# Database
DATABASE_URL=mysql://vibe:vibepw@localhost:13307/vibeenglish

# Auth.js
AUTH_SECRET=replace-with-openssl-rand-base64-32
AUTH_URL=http://localhost:3000

# Google OAuth (optional in phase 1 — leave blank to disable)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Bootstrap admin (set to an email; the user with this email gets role=admin on first sign-up)
ADMIN_EMAIL=admin@vibeenglish.local
