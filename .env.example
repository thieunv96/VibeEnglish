# Copy to .env and fill in real values.

# MariaDB (matches docker-compose.yml — `docker compose up -d` to provision)
DATABASE_URL="mysql://vibeenglish:vibepass@localhost:3307/vibeenglish"

# Auth.js — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET="change-me"
AUTH_TRUST_HOST=true
NEXTAUTH_URL="http://localhost:1998"
